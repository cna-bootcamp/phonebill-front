apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: phonebill-front
spec:
  ingressClassName: nginx
  rules:
  - host: phonebill.52.231.227.173.nip.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: phonebill-front
            port:
              number: 8080